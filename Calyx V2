---@diagnostic disable: undefined-global
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/FlintHacks/Calyx-V2/main/PF"))()

-- Window
local Window, MainGUI = Library:CreateWindow("Calyx")

-- Tabs
local LegitTab = Window:CreateTab("Legit")
local CharTab = Window:CreateTab("Character")
local RageTab = Window:CreateTab("Rage")
local EspTab = Window:CreateTab("ESP")
local VisualsTab = Window:CreateTab("Visuals")
local SettingsTab = Window:CreateTab("Settings")


local Legit = LegitTab:CreateGroupbox("Aim assist", "Left")
local Trig = LegitTab:CreateGroupbox("Triggerbot", "Right")
local Gun = LegitTab:CreateGroupbox("Gun behaviour", "Right")

local AntiAim = CharTab:CreateGroupbox("Anti aim", "Right")
local RageGun = RageTab:CreateGroupbox("Blatant", "Left")
local Grenade = RageTab:CreateGroupbox("Grenades", "Right")
local GunMods = RageTab:CreateGroupbox("Gun mods", "Right")

local Character = CharTab:CreateGroupbox("Character", "Left")

-- Legit aimbot
local AimbotToggle = Legit:CreateToggle("Enabled", function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local VisibleCheck = Legit:CreateToggle("Visible check", function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local AimbotFOV = Legit:CreateSlider("Aim assist fov", 0, 360, 90, function(value)
   
end)

local ShowFov = Legit:CreateToggle("Show FOV", function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local smoothing = Legit:CreateSlider("Smoothness", 0, 10 ,4, function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local smoothingToggle = Legit:CreateToggle("Smoothing",function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local AimbotHitChance = Legit:CreateSlider("Aimbot hit chance", 0, 100, 70, function(value)
   
end)

local HitboxToggle = Legit:CreateToggle("Enable hitboxes", function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local HitboxDrop = Legit:CreateDropdown("Hitbox extender", {"Head", "Left arm", "Right arm", "Torso", "Left leg", "Right leg", "All of the above"}, function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local HitboxSize = Legit:CreateSlider("Hitbox size (Studs)", 0, 10, 0, function(value)
   
end)

local HitPriority = Legit:CreateDropdown("Hit priority", {"Head", "Chest", "Legs"}, function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local AdjustForBulletDrop = Legit:CreateToggle("Adjust for bullet drop", function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local DontLockReload = Legit:CreateToggle("Turn off aimbot when reloading",function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local Deadzone = Legit:CreateSlider("Deadzone FOV", 0, 360, 121,function(state)
    
end)
-- Legit triggerbot

local TrigToggle = Trig:CreateToggle("Enabled",function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local InstantTrig = Trig:CreateToggle("Visible check",function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local TrigReaction = Trig:CreateSlider("Reaction time (ms)", 0, 400, 130, function(value)
   
end)

local AutoMan = Trig:CreateToggle("Hold for automatics",function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

local burstTrig = Trig:CreateToggle("Auto burst",function(state)
   if state then
        spawn(function()
            
        end)
    end
end)

-- Legit Gun Behaviour

local SiletAim = Gun:CreateToggle("Silent aim",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local silentAimFOV = Gun:CreateSlider("Silent aim FOV", 0, 1000, 130, function(value)
    
end)

local SilentHit = Gun:CreateSlider("Silent aim hit chance", 0, 100, 80, function(value)
    
end)

local RecoilReduction = Gun:CreateToggle("Recoil Reduction",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local RecoilX = Gun:CreateSlider("Recoil reduction (x)", 0, 100, 100, function(value)
    
end)

local RecoilY = Gun:CreateSlider("Recoil reduction (Y)", 0, 100, 100, function(value)
    
end)

local RecoilSpread = Gun:CreateToggle("No spread",function(state)
    
    if state then
        spawn(function()
            
        end)
    end
end)

local NoBulletDrop = Gun:CreateToggle("No bullet drop",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local NoSway = Gun:CreateToggle("No Sway",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local CombineMags = Gun:CreateToggle("Combine mags",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

-- anti aim

local AntiAimToggle = AntiAim:CreateToggle("Enable",function(state)
    if state then
        spawn(function()
            print("Anti aim enabled")
        end)
    end
end)

local Spinbot = AntiAim:CreateToggle("Spinbot",function(state)
    if state then
        spawn(function()
            print("Spinbot enabled")
        end)
    end
end)

local SpinSpeed = AntiAim:CreateSlider("Spin speed", 0, 100, 40, function(value)
    spawn(function()
        if value ~= 40 then
            if value == 40 then
                print("Spin speed not changed")
            end
        -- put something here when changed spinspeed
        end
    end)
end)

local DetachHead = AntiAim:CreateToggle("Detatch head", function(state)
    if state then
        spawn(function()
            print("head")
        end)
    end
end)

local ForceStanceToggle = AntiAim:CreateToggle("Force stance", function(state)
    if state then
        spawn(function()
           
        end)
    end
end)

local ForceStance = AntiAim:CreateDropdown("Force stance", {"Standing", "Crouching", "Prone" }, function(state)
    if state then
        spawn(function()
            print("Force stance changed")
        end)
    end
end)

local AxisSpoof = AntiAim:CreateToggle("Axis spoofer", function(state)
    if state then
        spawn(function()
            print("Spoofed axis")
        end)
    end
end)

local CycleToggle = AntiAim:CreateToggle("Cycle through all stances", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local Cyclespeed = Gun:CreateSlider("Cycle speed", 0, 30, 12, function(value)
    
end)

local Look = AntiAim:CreateToggle("Force look", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local LookDirection = AntiAim:CreateDropdown("Look direction", {"Up", "Down", "Left", "Right", "Randomize"}, function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

-- rage gun

local autoshoot = RageGun:CreateToggle("Auto shoot",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local autoRage = RageGun:CreateToggle("Hell mode",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local ChatSpam = RageGun:CreateToggle("Chat spam",function(state)
    if state then
        spawn(function()
            loadstring(game:HttpGet('https://pastebin.com/raw/JpXnMC61'),true)()
        end)
    end
end)
	
local spawnkill = RageGun:CreateToggle("Insta kill (Recommended)",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local killDelay = RageGun:CreateSlider("Kill delay (ms)", 0, 250, 0, function(value)
    if value then
        spawn(function()
        
        end)
    end
end)

local FullAuto = RageGun:CreateToggle("Full auto",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local prioritizecollats = RageGun:CreateToggle("Prioritize Collats",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local Mincollats = RageGun:CreateSlider("Minimum collat priority", 0, 5, 2, function(value)
    if value then
        spawn(function()
        
        end)
    end
end)

local Maxcollats = RageGun:CreateSlider("Maximum collat priority", 2, 10, 5, function(value)
    if value then
        spawn(function()
        
        end)
    end
end)

local autowallbang = RageGun:CreateToggle("Auto wallbang",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local  knifeaura = RageGun:CreateToggle("Knife aura",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local KnifeRadius = RageGun:CreateSlider("Knife aura radius", 0, 50, 25, function(value)
    if value ~= 25 then
        spawn(function()
        
        end)
    end
end)

local BulletRicochet = RageGun:CreateToggle("Bullet bounce (Beta)",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

-- grenades

local GrenadeEnabled = Grenade:CreateToggle("Spam grenades",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)


local GrenadeBudget = Grenade:CreateSlider("Grenade budget", 0, 3, 3, function(value)
    if value then
        spawn(function()
            
        end)
    end
end)

local GTP = Grenade:CreateToggle("Teleport grenades",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local QuickThrow = Grenade:CreateToggle("Quick throw",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

-- character

local Fly = Character:CreateToggle("Fly",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local Flyspeed = Character:CreateSlider("Fly speed", 0, 100, 50, function(value)
    if value then
        spawn(function()
            
        end)
    end
end)

local WalkspeedToggle = Character:CreateToggle("Modify walkspeed",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local Walkspeed = Character:CreateSlider("Walkspeed", 0, 100, 16, function(value)
    if value then
        spawn(function()
            
        end)
    end
end)

local JPToggle = Character:CreateToggle("Modify jumpspeed",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local JumpPower = Character:CreateSlider("Jump power", 0, 300, 6, function(value)
    if value then
        spawn(function()
            
        end)
    end
end)

local gravity = Character:CreateToggle("Modify gravity",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local Infinitejump = Character:CreateToggle("Inf jump",function(state)
    if state then
        spawn(function()
            InfiniteJumpEnabled = true
            game:GetService("UserInputService").JumpRequest:connect(function()
	            if InfiniteJumpEnabled then
		        game:GetService"Players".LocalPlayer.Character:FindFirstChildOfClass'Humanoid':ChangeState("Jumping")
	            end
            end)
        end)
    end
end)

local GravitySlider = Character:CreateSlider("Gravity", -10, 10, 1, function(value)
    if value ~= 1 then
        spawn(function()
            local gravityslidervalue = value
        end)
    end
end)

local NoFallDmg = Character:CreateToggle("No fall damage",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local BHOP = Character:CreateToggle("Bunny Hop", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local NoClip = Character:CreateButton("NoClip (E)",function(state)
    if state then
        spawn(function()
            noclip = false
            game:GetService('RunService').Stepped:connect(function()
                if noclip then
                game.Players.LocalPlayer.Character.Humanoid:ChangeState(11)
                end
            end)
            plr = game.Players.LocalPlayer
            mouse = plr:GetMouse()
            mouse.KeyDown:connect(function(key)
                if key == "e" then
                noclip = not noclip
                game.Players.LocalPlayer.Character.Humanoid:ChangeState(11)
                end
            end)
        end)
    end
end)

local Slidecool = Character:CreateToggle("No slide cooldown",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local jumpcool = Character:CreateToggle("No jump cooldown",function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local NoRespawning = Character:CreateButton("Disable respawning" ,function()

end)



-- gun mods

local SilentBullets = GunMods:CreateToggle("SilentBullets", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)


local GunBob = GunMods:CreateToggle("No gun bob", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)


local RemoveDelay = GunMods:CreateButton("Remove all delays", function(state)
    if state then
        spawn(function()
            print("345")
        end)
    end
end)


local InstantReload = GunMods:CreateToggle("Instant reload", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)

local activatebutton = GunMods:CreateButton("Apply gun mods", function(state)
    if state then
        spawn(function()
            
        end)
    end
end)
